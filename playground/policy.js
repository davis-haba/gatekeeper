/**
 * @fileoverview Description of this file.
 */
function policy(objStr, ParamStr) {
  console.log("testing")
  console.log(objStr)
  console.log(ParamStr)

  let output = "success"

  //var obj = Map(JSON.parse(objStr))
  //var params = Map(JSON.prase(ParamStr))

  //var labels = obj.get("metadata").get("labels")

  //params.get("labels").forEach(function (label, idx) {
    
  //});
}
